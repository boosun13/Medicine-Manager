// modalを閉じる処理
$("#newComment").find(".modal").modal("hide");
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();


// Rails @status => true 画面の書き換え : false モーダルを再度表示
// <% if @status %>
    $("#Post<%= @post.id %>").html("<%= j(render 'index', post: @post ) %>");
    
// <% else %>
    $("#newComment<%= @post.id %>").html("<%= j (render 'new') %>");

    //<% if @comment.errors.include?(:content) %>
        $('.field textarea').css({'background-color': '#fcc','padding': '5px','border-radius': '10px'});
    //<% end %>
// <% end %>