<nav class="fixed-top navbar navbar-expand-sm navbar-dark bg-dark" style="min-height:60px;">
    <%= link_to 'Medicine App', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">About</a>
            </li>
        </ul>
        
        <% if user_signed_in? %>
            <%= link_to "ユーザー詳細へ戻る", user_path(current_user) %>
            <%= link_to 'プロフィール変更', edit_user_registration_path %>
            <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
        <% else %>
            <div class="text-right nav-link">
            <button type="button" class="btn btn-primary p-1" data-toggle="modal" data-target="#loginModal">ログイン</button>
            </div>
            <div class="text-right nav-link">
            <button type="button" class="btn btn-primary p-1" data-toggle="modal" data-target="#signupModal">サインアップ</button>
            </div>
        <% end %>
    </div>
</nav>